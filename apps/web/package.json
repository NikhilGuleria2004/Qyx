{
  "name": "@qyx/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "lint": "eslint . --ext .ts,.tsx",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "deploy:preview": "pnpm build && wrangler pages project create qyx-web-preview --production=false || true && pnpm build && wrangler pages publish dist --project-name=qyx-web-preview",
    "deploy:staging": "pnpm build && wrangler pages project create qyx-web-staging --production=true || true && pnpm build && wrangler pages publish dist --project-name=qyx-web-staging",
    "deploy:prod": "pnpm build && wrangler pages project create qyx-web --production=true || true && pnpm build && wrangler pages publish dist --project-name=qyx-web"
  },
  "dependencies": {
    "@qyx/config": "workspace:*",
    "@qyx/crypto": "workspace:*",
    "@qyx/schemas": "workspace:*",
    "@qyx/ui": "workspace:*",
    "lucide-react": "^0.330.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^7.18.2",
    "zustand": "^4.5.0"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.2",
    "@testing-library/react-hooks": "^8.0.1",
    "@types/react": "^18.2.55",
    "@types/react-dom": "^18.2.19",
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.4.17",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.1",
    "typescript": "^5.3.3",
    "vite": "^5.1.0",
    "vitest": "^1.3.1",
    "wrangler": "^3.78.0"
  }
}
